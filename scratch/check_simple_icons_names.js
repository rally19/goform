const icons = require('simple-icons');
console.log(Object.keys(icons).filter(k => k.toLowerCase().includes('github') || k.toLowerCase().includes('twitter') || k.toLowerCase().includes('linkedin') || k.toLowerCase().includes('x')));
